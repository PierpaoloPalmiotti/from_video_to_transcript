# =====================================================================
# Dipendenze opzionali per accelerazione GPU NVIDIA (CUDA)
# =====================================================================
# Installazione (alza il timeout — sono pacchetti pesanti, ~500 MB ciascuno):
#   pip install --timeout 300 -r requirements-gpu.txt
#
# Questi pacchetti contengono le DLL CUDA (cuBLAS, cuDNN) necessarie
# a CTranslate2 / faster-whisper per usare la GPU. L'app le rileva
# automaticamente al primo avvio e le aggiunge al PATH.
#
# NOTA: il driver NVIDIA va installato comunque a livello di sistema.
# Aggiornalo da: https://www.nvidia.com/Download/index.aspx
#
# Se CUDA non è disponibile, l'app fa fallback automatico su CPU.
# =====================================================================

nvidia-cublas-cu12
nvidia-cudnn-cu12